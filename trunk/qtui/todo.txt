//Done
//4.5 bug
2 unterminated thread 4.5 bug?
2 sorting doesnt recover position?
/ crash on local game list

/ greatly improved GUI responsiveness
/ added ips dialog
/ moved M1 initialization to background thread
/ fixed parsing hex song # of M1 list
/ fixed core input automatic enable options

//Linux
2 bug: column sorting
mounted snap doesnt load
当使用linux版本的时候，rom和artworks等文件夹放在ntfs分区时，会在其目录下建立.mamepgui文件夹

//Dev
command.dat is slow and buggy
2 support mamep 127
2 ips dir must restart
2 icon for local switching
2 ips, m1 module availability detection for official
2 new version detection
export artwork dat
就是，检查当前DAT里文件，是否与当前的ROM名字对否，CRC对否。。。。- -一个个试真的太累了
export complete fixdat, current availabe only fixdat, selected, mame, mamep, mamep extra dat.

//Options
2 设置改动后，必须移动到下一行或者按Enter才能生效，直接关了就丢失
2 command.dat path in lang
1 add a menu to auto mkdir
1 add GUI Options +0.1
* customizable GUI text font / color editor +0.05
* driver config editor	+0.05
* -driver_config, mamebin changes should cause gamelist->init()
* option help
* vector.ini, debug.ini
* unify method to get inherited default value
* general info page

//GameList
2 provide a builtin fallback for zip folders
2 rewrite icon loading
2 crash on refresh GBA
2 如果快速的上下翻动游戏列表（按上下键），偶尔会出现跳出窗口提示内存错误的提示
* widget loaded prompt
* SDL joystick GUI
* export lst gamelist
1 7z binary path, doesnt stall when 7z is not available
1 rewrite icon loading for individual icon loading, restore item icon
1 play inp/rec/mng/wav...
* large icon view
* coin/time counter column
* sort by reading
* display 2 lines of game desc local/english
? context menus: for listview mode
* context menus: snapshot, folder
* in-place zooming for icons
* add/remove items from game list model
* ROM capa column
* unify multiple snapshot routines

//Folder
1 implement filter flags
1 favorite
* custom ini read/write +0.03
* dnd support +0.01
* other filters +0.03
* arc icos from old caname
* 读取分类列表，说明属于“格斗、赛车、解谜”之类的 +0.01
* bios icons
* only non-empty console/bios folders +0.02

//Snap
* split snap/history code
1 gamename0000 snap
* external default.png
* snap with or without stretching, horz/vert xml loading
* avi/mng playback
* mameinfo of driver

//M1
* check hardware capa

//Audit
2 crash on 128u2->u3? missing sets
2 auto audit when available = 0? +0.01
2 we can't assume nodump is not passed +0.03
2 check chd exist, check chd auditing (use mame core?) +0.03
2 right click auditing +0.02
* lost column state after auditing +0.01
* crash when exit and auditing
* non compressed roms
* return update thread immediately if some folders doesnt even exist

//Main Window
1 Add an option to revert the geometry to default, I lost a couple of my snap/info
2 F11 fullscreen mode, vista style

//IPS
1 ips +0.1
* 官方mame支持ips +0.1
* add verify IPS on RunMAME, remove it if N/A
* ips v4 +0.1

//Misc
* move datafile.c statistics to GUI
* s11n of detailed console extrom is not needed

//Console
1 console status isnt correct
* console folder icon
* multi software dirs
* console game info from GameFAQs?
* wildcard console gamename
  snapshot
  use mess hsi(goodxxx)
  use xmdb directly? (name based)
  add manufacturer from offline dat (name based, one time, submit to MESS)
  snap/snes/parent game name (without tags)
  display region/hack info of console rom

//Extra
* controller setting interface
* export roms to folders
* delete roms,cfg,nvram...
* delete snapshot
* gamelist editor
* export fixdat, selected fixdat only
* mess devices
* autoupdater

----------------------------------------

[sanity check]
* check new/delete operator
* disconn / conn pairs
* ensure ->load() functions only called once, no multiple connect()

[qt compile config]
windows:
configure -release -static -no-exceptions -no-accessibility -no-stl -no-phonon -no-webkit -no-scripttools -no-3dnow -no-sse -no-sse2 -no-openssl -no-rtti -qt-style-windowsvista -qt-style-plastique -qt-style-cleanlooks -no-style-motif -no-style-cde
linux:
libqt4-dev
libgtk2.0-dev

configure -release -static -no-exceptions -no-accessibility -no-stl -no-phonon -no-svg -no-webkit -no-scripttools -no-3dnow -no-sse -no-sse2 -no-openssl -no-opengl -sm -xinerama -xcursor -xfixes -xrandr -xrender -fontconfig -gtkstyle -nomake examples -nomake demos -nomake tools
macosx:
configure -release -static -no-exceptions -no-accessibility -no-stl -no-phonon -no-svg -no-webkit -no-scripttools -no-3dnow -no-sse -no-sse2 -no-openssl -nomake examples -nomake demos -nomake tools -universal -no-dwarf2

cd /mnt/hgfs/qtui

linux:
export PATH=/usr/local/Trolltech/Qt-4.5.0/bin:$PATH
mac:
export PATH=/Developer/usr/bin:$PATH

sudo cp -f mamepgui /usr/games
sudo chmod +x /usr/games/mamepgui

[mamepgui test]
1 dock snap

[mamep core test]
ips does not inherit
DRC core
// mamep: dont save settings during playback
// dont save nvram during playback
load nvram from playback dir
neogeo playback rtc

[mamep core bug]
mamep snaps
hazemd icons

verify spclone dips
gba no sound

add makelang text, src repo link on main page

ftp://ftp.trolltech.com/qt/snapshots/qt-all-opensource-src-4.5.0-snapshot-20080918.tar.gz