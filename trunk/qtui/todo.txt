//Web
render xml feed
multi lang basic UI
read/write cookie
theme

//Done

//Linux/Mac
D column sorting
2 On OSX, would it be possible to make the front end start the selected game when you press return?
check font size
icon最大可以大到512x512 Apple也强烈建议开发者要准备这么大的尺寸（除了原有的16x16、32x32、128x128之外 
Windows在XP以前仅支持16x16、32x32、48x48，直到Vista才开始加大到64x64和256x256。
当使用linux版本的时候，rom和artworks等文件夹放在ntfs分区时，会在其目录下建立.mamepgui文件夹
能不能价格功能，让写入配置文件的时候输入root密码拥有写入权限  

//Dev
group hu_hu
2 try to load a default bkgr
2 m1能用，但是有m1在bin目录首次启动新龟启动不了
>LIN下的GUI能不能想办法支持汉化列表

2 crash: I try to delete Cabinet THEN browese. It closes program
2 global softwarepath
1 submit joyid
4 verify rev 3991

1 MESS skip warning
2 multi sw paths
1 MESS cfg saving
1 ramoptions

2 disable play with
1 reduce auto audit calls
1 playwith state name prompt
1 filter also display parent (cross line?)
2 chd clones
2 several hundred games are no more seen as complete because zip files don't include bios files.
1 unavail folder, parent avail, clone unavail
2 MAME uses the M1 directory as currentdirectory, mame option available after m1 init
1 treeview bug,能调到比默认宽,不能调窄
2 I keep all files on a separate drive from the mame executable and the UI doesn't display sna
2 provide a builtin fallback for zip folders
2 mamep core lang, for non cjk languages
2 gamename0000 snap
2 custom ini write
1 multiple software dirs1 multiple software dirs
1 SDL joystick GUI
1 default command.dat path in lang
1 driver config editor, -driver_config, mamebin changes should cause gamelist->init()
1 ips dialog size
1 option not loaded before m1 init

* use size()
* folder selected game hash cache [8]
* play with dlg, empty file name handling
* test oldest mame version support, warning
* 残档目录
* translator credit
* screenshot.cpp
* bios filter really slow
1 icons\warning.ico for the non-working games.
1 snap\nonworking.png for non-working games snap display
* remove TreeItem
* separate game list language
* reconsider command.dat it's slow and buggy
* icon for local switching
* new version detection
* 检查当前IPS DAT里文件，是否与当前的ROM名字对否，CRC对否。。。。- -一个个试真的太累了
* check new/delete operator
* move datafile.c statistics to GUI

//Main Window
1 Add an option to revert the geometry to default, I lost a couple of my snap/info
1 F11 fullscreen mode, vista style

//Options
1 check default bios since .129
* support mamep 127 paths
* add a menu to auto mkdir
* unify get GUI opts default
* keycode ctrlr config editor
* customizable GUI text font / color editor +0.05
* option helper from -su
* vector.ini, debug.ini
* unify method to get inherited default value
* general info page

//GameList
* manuals support planned? (http://www.mamext.net/)
* Ctrl-F w/ functionality to filter like MameUI including its text filter.
* inherit parent history.dat
* export lst gamelist
* coin/time counter column
* sort by reading
* display 2 lines of game desc local/english
* in-place zooming for icons
* add/remove items from game list model
* ROM capa column

//Audit
* audit samples
* clrmame style dialog
1 gba F5 crash
1 audit all
* crash when exit and auditing
* non compressed roms

//Folder
1 rewrite filter by thread
* other filters
* 读取分类列表，说明属于“格斗、赛车、解谜”之类的 +0.01
* bios icons
* dnd support

//Snap
* external default.png.0
* horz/vert .lay loading
* avi/mng playback
* mameinfo of driver

//M1/IPS
* check m1 hardware capa
* ips dir must restart
* ips v1 dat fallback with datfile name
* 官方mame支持ips +0.1
* multi directory

//MESS
1 buggy sorting console list
* if we are in an empty MESS system folder, assign currentGame to the system
* console folder icon
* multi software dirs
* console game info from GameFAQs?
* wildcard console gamename
  snapshot
  use mess hsi(goodxxx)
  use xmdb directly? (name based)
  add manufacturer from offline dat (name based, one time, submit to MESS)
  snap/snes/parent game name (without tags)
  display region/hack info of console rom

//Extra
* controller setting interface
* export complete fixdat, current availabe only fixdat, selected, mame, mamep, mamep extra dat, snapshot dat
* export roms to folders
* delete roms,cfg,nvram...
* delete snapshot
* gamelist editor
* autoupdater

----------------------------------------

[qt compile config]
windows:
configure -release -static -no-exceptions -no-accessibility -no-stl -no-phonon -no-webkit -no-scripttools -no-3dnow -no-sse -no-sse2 -no-openssl -no-rtti -qt-style-windowsvista -qt-style-plastique -qt-style-cleanlooks -no-style-motif -no-style-cde
linux:
libqt4-dev
libgtk2.0-dev
configure -release -static -no-exceptions -no-accessibility -no-stl -no-phonon -no-svg -no-webkit -no-scripttools -no-3dnow -no-sse -no-sse2 -no-openssl -no-opengl -sm -xinerama -xcursor -xfixes -xrandr -xrender -fontconfig -gtkstyle -nomake examples -nomake demos -nomake tools
macosx:
configure -release -static -no-exceptions -no-accessibility -no-stl -no-phonon -no-svg -no-webkit -no-scripttools -no-3dnow -no-sse -no-sse2 -no-openssl -no-dwarf2 -nomake examples -nomake demos -nomake tools

cd /mnt/hgfs/qtui

linux/mac:
export PATH=/usr/local/Trolltech/Qt-4.5.0-beta1/bin:$PATH

sudo cp -f mamepgui /usr/games
sudo chmod +x /usr/games/mamepgui

[mamepgui test]
in an empty folder
1 dock snap
w/ or w/o m1
check sorting
mame, mamep in same folder
gui/core options saving
bios, ctrlr, effect, screen
path w/ space and Unicode

[mamep core test]
ips should not inherit
// mamep: dont save settings during playback
// dont save nvram during playback
load nvram from playback dir
neogeo playback rtc

[mamep core bug]
remove nvram hack
mamep snaps
hazemd icons
verify spclone dips
add makelang text, src repo link on main page
