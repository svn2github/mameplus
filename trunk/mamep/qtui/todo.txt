[basic]
/ settings dialog 0.7
/ proper ini entries handling 0.8
/ reset settings button 0.75
1 start mame core 0.85
1 all filters: 0.9 orientation, manu, year, driver, bios, cpu, sound, emu status, dumping status, working, not working, orignals, clones, raster, vector, resolution, fps, savestate, control type, stereo, chd, samples, artwork
1 unify method to get inherited default value
1 replace optview with custom render
1 bios support: audit selected bios only, bios.ini(biosof(), top romof isbios()), select bios (loop rominfo, see bios="")
* vector.ini, source/<sourcefile>.ini if that doesn't exist, try <sourcefile>.ini
* general info page
* switch tree/combobox
* path chooser dialog
* more responsive threading
* background image
* ips
* UI style: compact, full, MAMEUI classic
* helpid
* fullscreen switch
* adjust snap with a/r
* if ini readonly, cannot read cache

[extension]
* export roms to folders
* delete roms,cfg,nvram...
* export fixdat, selected fixdat only
* ROM capa column
* gamelist editor
* selectable l&f
* multiple snapshot
* delete snapshot
* hide to systray when lauching mame
* search history
* mess console game list
* loading gif
* mng playback
* command.dat parsing with image
* controller setting

[sanity check]
* check new/delete operator
* ensure ->load() functions only called once, no multiple connect()
