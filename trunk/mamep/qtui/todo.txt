[bug]
1 CJK path encoding

[qt 4.4]
QAbstractItemView Add autoScrollMargin property.    * [162547] Make the current index stay in the viewport when sorting.
QTreeView 192104] scrollTo(PositionAtCenter) can scroll beyond the item if item is at 0

[todo]

BETA
bug: few select model
1 game counter in statusbar
1 load default layout from preset ini
1 preserve audit result when mamever changes

1 could be cool to have behind the XX% - "name of the rom beeing audited"
1 add/remove items from game list model
bug: update options from right to left
reduce restore sele //win->log("restore callback: " + gameName);
1 -driver_config
1 bios icons in folder list
1 ips doesnt inherit

* bios support: select bios opt
1 path chooser dialog +0.1
1 about page, some link to the test board
1 lst translation +0.1
1 ips +0.2
1 command.dat parsing with image +0.2
* disable filters for 3D hardware
* skip or delay invisible widget loading)
* adjust snap with a/r, with or without stretching
* iconized snap tab
* onclick to select ss tab
1 return update thread immediately if some folders doesnt even exist
* display different colors for different status
* console list auditing
C replace optview with custom render
C unify method to get inherited default value
* multi ini path
* vector.ini, source/<sourcefile>.ini if that doesn't exist, try <sourcefile>.ini
* other filters
* gamename0000 snap
* general info page
* UI style: compact, full, MAMEUI classic
* non compressed roms
* helpid
* fullscreen switch
* move datafile.c statistics to GUI

[extension]
* export roms to folders
* delete roms,cfg,nvram...
* export fixdat, selected fixdat only
* ROM capa column
* gamelist editor
* selectable l&f +0.2
* unify multiple snapshot
* delete snapshot
* hide to systray when lauching mame
* search history
* avi/mng playback
* controller setting
* SDL integration for monitor/joystick

[sanity check]
* check new/delete operator
* ensure ->load() functions only called once, no multiple connect()

[ads]
auditing roms several times faster than old mame32

[config]
configure -static -no-exceptions -no-rtti -no-stl -no-style-plastique -no-style-cleanlooks -no-style-motif -no-style-cde

[test]
ips does not inherit
DRC core
// mamep: dont save settings during playback
// dont save nvram during playback
load nvram from playback dir
neogeo playback rtc

[mamep core bug]
cfg volume_multiplier always presents

[snapshot]
args:
dirpath, zipname, extname, gamename

if !empty
  return 

iteratre split dirpath
{
  load path + gamename
  if empty, add zip
  if empty, add subdir
  if empty recursively call cloneof
  if empty recursively call romof?
}

if empty return default